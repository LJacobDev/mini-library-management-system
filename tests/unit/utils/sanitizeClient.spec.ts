import { describe, it, expect } from 'vitest';
import { sanitizeText } from '/app/utils/sanitizeClient';

// starting test function to check test runner
describe ('sanitizeText', () => {
    it('test', () => {
        expect(1).toBe(1);
    })
});